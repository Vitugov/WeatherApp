@model List<CityWeather>

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-lg-3">
                @{
                    await Html.RenderPartialAsync("_CityWeatherCard", item);
                }
            </div>
        }
    </div>
</div>
