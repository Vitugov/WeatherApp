@model CityWeather

@{
    var viewData = new ViewDataDictionary(ViewData)
    {
        { "UseDefaultLink", false },
        { "Link", "~/" }
    };
}

<div class="container">
    <div class="row">
        <div class="col-lg-3">
            @{
                await Html.RenderPartialAsync("_CityWeatherCard", Model, viewData);
            }
       </div>
    </div>
</div>
