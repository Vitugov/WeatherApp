@model CityWeather

@{
    ViewBag.Link = "~/";
}

<div class="container">
    <div class="row">
        <div class="col-lg-3">
            @{
                await Html.RenderPartialAsync("_CityWeatherCard", Model);
            }
       </div>
    </div>
</div>
